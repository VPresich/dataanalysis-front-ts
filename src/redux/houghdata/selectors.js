export const selectHoughData = (state) => state.hough.items;
export const selectIsLoading = (state) => state.hough.isLoading;
export const selectError = (state) => state.hough.error;
export const selectResult = (state) => state.hough.result;
export const selectResult3D = (state) => state.hough.result3d;
